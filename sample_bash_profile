
# rbenv section of bash_profile
# disable rbenv for now, using chruby instead. . .

# export PATH="$HOME/.rbenv/bin:$PATH"
# eval "$(rbenv init -)"

# end rbenv sectino of bash_profile


# Aliases: http://ryan.mcgeary.org/2011/02/09/vendor-everything-still-applies/

alias b="bundle"
alias bi="b install --path vendor"
alias bil="bi --local"
alias bu="b update"
alias be="b exec"
alias binit="bi && b package && echo 'vendor/ruby' >> .gitignore"


# Also source bashrc in addition to this content in bash_profile
# (See http://stackoverflow.com/questions/415403/whats-the-difference-between-bashrc-bash-profile-and-environment)

[[ -r ~/.bashrc ]] && . ~/.bashrc

# end bash_profile
